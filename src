import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.tsx';
import './index.css'; // Importa o arquivo de estilos (incluindo Tailwind CSS)

// Pega o elemento 'root' que está no index.html e renderiza o componente App dentro dele
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
